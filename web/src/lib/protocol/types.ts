// Keep this stable import path for application code. The structural protocol
// types themselves are generated by play-room-protocol.
export type {
  ClientEnvelope,
  ClientRequest,
  ErrorCode,
  GameKind,
  GameRules,
  Id,
  Move,
  PlayerId,
  PlayerRole,
  PlayerScore,
  PlayerView,
  RoomEvent,
  RoomId,
  RoomPhase,
  RoomSnapshot,
  RoomSummary,
  RoundEndReason,
  RoundOutcome,
  RoundResult,
  ServerEvent,
  ServerMessage,
  ServerResult,
  SessionToken,
  WelcomeState,
} from './generated-types';